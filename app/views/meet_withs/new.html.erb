<%= form_with(model: @meet_with, url: meet_withs_path, local: true) do |f|%>

<h3 class="field">
<%= f.label :text, "新規投稿内容" %><br />
    <%= f.text_area :text %>
  </h3>

<div class="actions">
    <%= f.submit "投稿する", class: :form__btn  %>
  </div>

<% end %>